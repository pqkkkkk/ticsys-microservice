spring:
    application:
        name: notification-service
    mail:
        host: smtp.gmail.com
        port: 587
        username: ticsys854@gmail.com
        password: zdquzkxizumihdhf
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
grpc:
    server:
        port: 9091
        
server:
  port: 8090


eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
        register-with-eureka: true
        fetch-registry: true
    instance:
        prefer-ip-address: true
        hostname: localhost
        ip-address: 127.0.0.1
        instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}
        metadata-map:
            grpc.port: "${grpc.server.port}"